# Let's Score a Bowling Game!

To work on this further on your own:

1. Clone this repo: `git clone git@github.com:derekhouck/bowling-exercise.git`
1. Install the dependencies: `npm install`
1. Open the `test/bowlingGame.js` file.
1. To run the tests, run `npm test` in a terminal.

